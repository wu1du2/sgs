# 曹髦技能实现细节（plan2）
本计划仅描述实现细节与落点，不直接修改代码。目标是为曹髦的六个技能提供可执行的实现方案与数据结构设计，符合当前项目“面杀模拟器”定位与现有结构约束。

## 0. 接入位置总览
- 技能逻辑文件：新增 `src/skills/caomao.js`，导出 `caomaoSkill`（包含通用 helper 与可复用逻辑）。
- 逻辑接入：在 `src/Game.js` 引入并在 `moves` 中挂载。
- UI 接入：在 `src/Board.jsx` 的 `onSkillClick` 和 `renderPlayerArea` 中接入按钮与动态展示。
- 测试名单：将“曹髦”加入 `TESTING_GENERAL_LIST` 以便测试。

## 1. 数据结构与公共工具
### 1.1 新增状态字段（players）
- `daoxin`: number，道心值，默认 0，上限 99。
- `caomaoUnlocks`: string[]，已解锁的阈值技能名（或直接用 `grantedSkills` 判定）。
- `caomaoFangzhuLastTarget`: string|null，上次放逐目标。
- `caomaoJuejinUsed`: boolean，决进是否已用。

### 1.2 新增状态字段（G）
- `caomaoQingzheng`: 多阶段状态机对象
  - `active`, `stage`, `sourceID`, `selectedSuit`, `targetID`, `targetSuit`, `sourceDiscardCount`, `targetDiscardCount`, `revealedTargetCards`
- `caomaoFangzhu`: 多阶段状态机对象
  - `active`, `stage`, `sourceID`, `targetID`, `option`
- `caomaoJuejinAuraActive`: boolean，向死存魏光环是否生效
- `exiledCards`: array，移出游戏的牌（全局）

### 1.3 公共 helper（放在 caomao.js 内）
- `addDaoXin(G, playerID, amount)`: 叠加道心并封顶 99。
- `checkDaoXinUnlocks(G, playerID)`: 道心达到阈值时授予技能（写入 `grantedSkills`）。
- `grantSkillIfMissing(G, playerID, skillName)`: 去重写入 `grantedSkills`。
- `logDaoXin(G, playerID, delta, reason)`: 统一写入 actionLog。
- `clampArmor(player)`: 护甲值任何时候不得超过 5（也不得小于 0）。

### 1.4 全局不变量（体力/护甲必须正确反映）
- 任何导致体力或护甲变化的结算必须直接写入对应 `G.players[targetID].hp / hpMax / armor`，并能在各武将区域实时展示。
- 护甲值任何时候都必须满足 `0 <= armor <= 5`：
  - 伤害结算（扣护甲）已在 `modifyHP` 内处理；新增任何“加护甲/设护甲/调整护甲”的逻辑都必须调用 `clampArmor`。
  - 决进等一次性全场结算必须对每名角色逐个封顶（包括“曹髦额外 +2”也受 5 上限约束）。

## 2. 潜龙（持恒技）
### 2.1 触发点与加值规则
- 开局：获得 20 点道心。
- 受到 1 点伤害后：道心 +10（自动）。
- 造成 1 点伤害后：道心 +15（手动点击一次）。
- 获得牌时：道心 +5（手动点击一次）。

### 2.2 落点
- 开局初始化：在 `selectGeneral` 完成后若武将为曹髦，设置 `daoxin` 初值并写日志。
- 受伤：在 `modifyHP` 的受伤分支中，若目标为曹髦，自动加道心。
- 造成伤害：提供“手动加道心”按钮，每次点击 +15 并写日志。
- 获得牌：提供“手动加道心”按钮，每次点击 +5 并写日志。

### 2.3 阈值解锁
- 25/50/75/99：分别授予“清正/酒诗/放逐/决进”。
- 解锁后写入 `grantedSkills`，UI 将自动展示。

## 3. 清正（持恒技）
### 3.1 流程拆解（用户选择 + 自动结算）
- 出牌阶段开始时自动弹出交互，但需要用户完成“选牌”与“选目标/选花色”。
- “全自动”的含义：在用户做出选择后，系统自动完成弃牌、伤害与道心（不需要再点确认多次）。
- 步骤：
  1) 曹髦选择一种花色（仅可选自己手牌中存在的花色）。
  2) 系统自动弃置曹髦手牌中该花色全部牌，并记录弃置数 `A`；同时若需要，道心按“获得牌/受伤/造成伤害”的规则由按钮或自动逻辑结算（见潜龙）。
  3) 曹髦选择一名有手牌的其他角色为目标。
  4) 系统展示目标的手牌花色分布（不必展示具体牌面），曹髦选择一种花色。
  5) 系统自动弃置目标手牌中该花色全部牌，并记录弃置数 `B`。
  6) 若 `B < A`，系统自动对目标造成 1 点伤害，并写入 actionLog。

### 3.2 状态机与 UI
- 使用 `G.caomaoQingzheng` 分阶段：
  - `select_suit_self` → `select_target` → `select_suit_target` → `resolve`
- UI 组件：
  - 花色选择弹窗：显示可选花色按钮（♥/♦/♠/♣）。
  - 目标选择：复用现有“点选角色”交互。
  - 目标花色选择：仅展示目标手牌的花色计数（例如 ♥x2 ♦x1），供选择。
- 自动结算触发点：当 `select_suit_target` 完成后立即进入 `resolve` 并一次性完成弃牌与伤害。

### 3.3 关键实现点
- 弃牌实现：从手牌中过滤花色并移入弃牌堆，记录数量。
- 伤害实现：调用 `modifyHP` 完成伤害。
- 道心处理：
  - 因清正造成的伤害不需要点击，直接+15

## 4. 酒诗（持恒技）
### 5.1 触发点
- 点击技能按钮后发动并翻面。
- 受到伤害后，若武将牌背面朝上且伤害时也为背面，翻回正面。
- 每次翻面后，获得一张锦囊牌（逻辑与“杨彪-让节”一致，增量复用）。

### 5.2 实现细节
- 翻面状态使用 `is_turned_over` 字段。
- 点击技能按钮时：
  - 新增 move `caomaoJiuPoemActivate`，若曹髦为正面则翻面并写日志。
  - 翻面后触发“获得锦囊牌”逻辑。
- 获得锦囊牌逻辑（复用让节逻辑）：
  - 参照 `rangjieChooseOption` 的“锦囊”分支，洗牌后从牌堆中按顺序找到一张锦囊牌（排除延时锦囊：乐、兵、电），放入手牌。
  - 若牌堆无可用锦囊则写入 actionLog。
  - 保持增量实现，不改动杨彪现有逻辑。

## 5. 放逐（持恒技）
### 6.1 触发点与限制
- 出牌阶段限一次，主动发动。
- 不能选择上一次放逐的目标。

### 6.2 状态机与 UI
- 使用 `G.caomaoFangzhu` 分阶段：
  - `select_target` → `select_option` → `resolve`
- 选项弹窗提供两项：
  - “技能失效直到其下回合结束”
  - “不能使用除锦囊牌以外的手牌直到其下回合结束”

### 6.3 仅记录日志
- 本次仅记录日志，不实现实际效果落地。
- 在 `resolve` 阶段写入 actionLog，记录选择的目标与选项，并更新 `caomaoFangzhuLastTarget`。
- 不提供任何“解除放逐”按钮或效果标记。

## 6. 决进（持恒技，限定技）
### 7.1 触发点
- 出牌阶段，点击技能后全自动发动。

### 7.2 结算流程
- 全场依次将体力值调整为 1。
- 计算护甲增量：`X = 体力减少值`，并对每名角色逐个封顶到 5；曹髦额外 +2（同样封顶 5）。
- 赋予“向死存魏”光环：
  - 将牌堆、弃牌堆、场上、所有角色区域中的【闪】【桃】【酒】移出游戏。
  - 将移出牌写入 `G.exiledCards` 以便追溯。
  - 明确移除弃牌堆中的【闪】【桃】【酒】。

### 7.3 光环持续规则
- `G.caomaoJuejinAuraActive = true` 后：
  - 每次牌进入任意区域时检查是否为【闪】【桃】【酒】，若是则移入 `exiledCards`。
  - UI 不再显示这些牌（不进入手牌/场上）。

### 7.4 技能使用限制
- 使用后写 `caomaoJuejinUsed = true`，`onSkillClick` 中禁止重复发动。

## 7. UI 显示与交互细节
- 曹髦技能按钮：在 `onSkillClick` 中加入 `潜龙/酒诗/放逐/决进` 的入口。
- 清正：出牌阶段开始时自动弹出交互入口（不在技能区额外加按钮）。
- 翻面 UI：在武将头像上加半透明阴影覆盖，背面时启用（可用 `position: absolute` 遮罩层）。
- 道心 UI：
  - 在武将区域左侧紧贴显示“水银温度计”样式竖条。
  - 刻度固定 25/50/75/100，显示刻度线与数字。
-  - 顶部提供 `+15` 与 `+5` 两个按钮，分别触发“造成伤害手动加道心”与“获得牌手动加道心”。
  - 竖条高度与道心值联动（0–100 线性映射）。

## 8. 随机数与一致性
- 任何“随机获得锦囊牌”“随机选择牌”必须使用 `ctx.random`，不得使用 `Math.random`。

## 9. 验证与手工用例
- 潜龙：受伤后道心自动增加；造成伤害后点击 +15 生效；获得牌后点击 +5 生效；阈值技能自动显示。
- 清正：出牌阶段开始弹出花色/目标/目标花色选择；选择完成后自动弃牌与结算伤害，日志完整。
- 酒诗：点击技能翻面并触发获得锦囊；背面受伤后翻回并再次获得锦囊。
- 放逐：可选目标与选项，日志记录完整，无实际效果落地。
- 决进：点击后全场体力调整、护甲封顶；弃牌堆中【闪】【桃】【酒】被移除。
