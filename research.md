# 深入研究报告 (In-depth Research Report)

本报告基于对 `card-game` 目录的深入分析，涵盖代码库结构、通知系统、任务调度机制以及已识别的取消逻辑缺陷。

## 1. 代码库结构深度解析 (Codebase Structure)

该项目是一个基于 **React** (前端) 和 **boardgame.io** (游戏逻辑/后端) 的卡牌游戏实现。

### 核心目录与文件职责
- **`src/Game.js`**: 
  - **核心逻辑**: 定义了游戏的状态 (`G`)、动作 (`moves`) 和回合流程 (`turn`)。
  - **状态机**: 管理所有游戏内状态，包括玩家状态、手牌、装备区、判定区以及各种临时技能状态（如 `liyuTargeting`, `taoluan`, `mizhao` 等）。
  - **规则引擎**: 处理出牌、摸牌、判定、伤害结算等基础规则。

- **`src/Board.jsx`**: 
  - **UI 渲染**: 负责渲染游戏界面，包括玩家区域 (`HeroArea`)、手牌区、日志区 (`ActionLog`) 和各种交互弹窗 (`CardSelectionModal`, `TargetSelectionPrompt`)。
  - **交互逻辑**: 监听用户点击事件，调用 `moves` 触发游戏动作。
  - **动画**: 处理简单的视觉动画（如受击、选中效果）。

- **`src/skills/*.js`**: 
  - **技能模块化**: 每个武将的技能逻辑被拆分到单独的文件中（如 `jielubu.js` 对应界吕布，`xuyou.js` 对应许攸）。
  - **状态管理**: 技能文件直接操作 `G` 对象，定义了技能相关的特定 moves（如 `useLiyu`, `taoluanStart`）。

- **`server.js`**: 
  - **服务端入口**: 使用 Koa 和 boardgame.io Server 搭建后端，处理多人游戏连接和状态同步。
  - **API**: 提供简单的 API（如 `/api/reset`）用于重置游戏数据。

- **`configs/generals.json`**: 
  - **配置数据**: 定义了所有武将的属性（体力、技能描述、启用状态等），是游戏初始化的基础数据源。

## 2. 通知系统详解 (Notification System)

游戏内的通知系统并非基于发布-订阅模式的即时消息服务，而是基于 **共享状态日志** 的被动渲染机制。

### 核心机制
- **数据源 (`G.actionLog`)**: 
  - `Game.js` 中的 `G` 对象包含一个 `actionLog` 数组。
  - 所有的游戏事件（出牌、造成伤害、技能发动等）都会由后端逻辑（`moves`）向此数组 `push` 一条字符串消息。
  - 示例: `G.actionLog.push("Player 0 使用了 杀")`。

- **UI 呈现 (`ActionLog` & `ActionTicker`)**:
  - **ActionLog 组件**: 位于 `Board.jsx`，实时监听 `G.actionLog` 的变化。它负责将日志渲染为列表，支持展开查看历史记录。
  - **ActionTicker 组件**: 用于在界面上方显示最新的一条日志，提供即时的视觉反馈。
  - **文本高亮**: UI 组件会对日志文本进行解析，自动识别卡牌名称（如 "杀", "桃"）和花色（♠, ♥, ♣, ♦），并应用特定的颜色样式（金色、红色、黑色等），增强可读性。

### 特点
- **一致性**: 由于日志是游戏状态的一部分，所有客户端看到的日志完全同步。
- **持久化**: 日志随游戏状态保存，断线重连后可完整恢复历史记录。

## 3. 任务调度机制 (Task Scheduling)

本项目 **没有使用传统的定时任务调度器**（如 `cron` 或 `setTimeout` 驱动的后台任务），而是采用了 **确定性状态机 (Deterministic State Machine)** 的调度模式。

### 状态驱动的阻塞式调度
- **阶段锁定**: 
  - 游戏流程由一系列“阻塞状态”控制。例如，当玩家发动“五谷丰登”时，游戏进入 `harvestCountSelect` 状态。
  - 在此状态解除前，游戏的主流程（如回合结束、阶段推进）被挂起，只有特定的 `move`（如 `selectHarvestCount`）能改变此状态。

- **隐式调度**: 
  - `Game.js` 中存在多个 `advance*` 函数（如 `advanceTaoluanAfterResolution`）。
  - 这些函数通常在 `moves` 执行完毕后被调用，或者作为 `turn.onMove` 的一部分，检查当前状态是否满足推进条件。
  - 如果满足条件（如“所有人都拿了五谷丰登的牌”），状态机自动推进到下一个逻辑节点。

- **风险**: 
  - 这种机制严重依赖状态的 **正确清理**。如果一个 `move`（特别是“取消”操作）未能完整清理当前阻塞状态，游戏将永久卡死在当前阶段，或者允许玩家在非法状态下执行后续动作。

## 4. 取消缺陷分析 (Cancellation Bugs Analysis)

通过代码审计与 `plan1.md` 的交叉验证，确认系统在处理“多阶段技能取消”时存在高危缺陷。这些缺陷主要表现为：**取消操作未彻底回滚状态或副作用，导致游戏逻辑处于不一致状态。**

### 4.1 利驭 (Liyu) - 状态残留导致流程未终止
- **缺陷描述**: 
  - 界吕布发动“利驭”后，流程分为“选择目标” (`liyuTargeting`) 和“选择卡牌” (`liyuCardSelecting`) 两个阶段。
  - 现有的取消逻辑（`cancelLiyuTarget`）主要关注“选择目标”阶段的清理。
  - 如果玩家在进入“选择卡牌”阶段后点击取消，若 UI 仅调用了 `cancelLiyuTarget` 且后端未正确级联清理 `liyuCardSelecting`，或者前端弹窗未正确响应状态变化，会导致“选牌弹窗”虽然关闭，但后端状态仍认为玩家处于选牌中。
- **后果**: 
  - 玩家可能在取消后，依然能触发“获得卡牌”的逻辑，或者在后续回合中意外触发上一回合残留的选牌逻辑。
- **修复方向**: 确保 `cancelLiyuTarget` 显式将 `liyuTargeting` **和** `liyuCardSelecting` 同时置为 `null`，并在前端确保取消按钮调用此 move。

### 4.2 滔乱 (Taoluan) - 取消越权 (Privilege Escalation)
- **缺陷描述**: 
  - 张让的“滔乱”技能允许玩家将一张牌当做另一张牌使用。流程涉及“出牌” -> “产生效果” -> “需队友配合/对方响应”。
  - 当前的 `taoluanCancel` 逻辑过于简单，未检查当前所处的子阶段。
  - 玩家可以在已经打出牌并产生效果（甚至已经进入需要对方响应的阶段）时，强行调用取消。
- **后果**: 
  - 玩家可以通过“取消”来跳过技能的代价（如跳过“队友给牌”或“自身掉血”），但技能产生的正面效果（如伤害）可能已经生效或进入结算堆栈。这属于严重的规则漏洞。
- **修复方向**: 为 `taoluanCancel` 增加 **阶段白名单** (Allowlist)。仅允许在“选牌”或“选目标”阶段取消；一旦进入“等待响应”或“结算”阶段，禁止取消。

### 4.3 密诏 (Mizhao) - 副作用未回滚 (No Rollback of Side Effects)
- **缺陷描述**: 
  - 献帝的“密诏”技能包含“将所有手牌交给目标A” (`confirmTargetA`) -> “A与B拼点” (`selectB`) 两个步骤。
  - 如果在第二步（A与B拼点阶段）点击取消，`cancelMizhao` 仅仅是重置了技能状态标记。
  - **关键问题**: 此时“将手牌交给A”的动作 **已经发生且未被回滚**。
- **后果**: 
  - 献帝发动密诏，把牌给了A，然后点取消。结果是：密诏结束，A白拿了献帝的所有牌，且不需要执行拼点。这完全破坏了游戏平衡。
- **修复方向**: 
  - **方案A (推荐)**: 在手牌移交后 **禁止取消**。一旦确认目标A，流程必须走完。
  - **方案B (复杂)**: 实现深度回滚。在取消时，将A手中的特定牌归还给献帝（需追踪牌的ID，且处理A可能已经用掉牌的复杂情况，风险极高）。

## 总结
当前系统的核心隐患在于 **状态机流转的可逆性管理不足**。
1.  **利驭**: 属于“清理不彻底”。
2.  **滔乱**: 属于“时机未校验”。
3.  **密诏**: 属于“副作用未回滚”。

建议优先修复 **滔乱** 和 **密诏** 的逻辑，限制取消的时机，确保游戏规则的严肃性；随后修复 **利驭** 的状态清理问题，提升用户体验。
