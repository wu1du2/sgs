# 开发规则与注意事项

## 武将技能开发规范

1.  **忽略 `skills_description` 字段**：
    在开发新武将或修改现有武将技能时，**严禁**参考 `configs/generals.json` 中的 `skills_description` 字段。该字段的内容可能不准确或不符合当前需求。

2.  **遵照用户命令**：
    所有技能逻辑的实现必须严格遵照用户的直接命令和描述。如果用户没有提供具体的技能细节，应主动询问，而不是依赖配置文件中的描述。