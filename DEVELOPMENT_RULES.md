# 开发规则与注意事项

1. **保持现有逻辑完整性**：任何新改动都不能影响已有的逻辑。如果必须修改现有逻辑，必须提前告知并说明原因。

## 核心游戏机制 - CRITICAL

**本游戏为“面杀模拟器”，规则与标准三国杀不同：**

1.  **无阶段限制 (No Phases)**:
    - 游戏**没有**判定阶段、摸牌阶段、出牌阶段、弃牌阶段等概念。
    - **严禁**在代码中实现阶段检查（如 `if (ctx.phase !== 'play')`）。
    - 玩家可以在任何时候进行任何操作（只要符合当前 UI 交互逻辑）。

2.  **无次数限制 (No Usage Limits)**:
    - 技能和卡牌**没有**“每回合限一次”或“出牌阶段限一次”的限制。
    - **严禁**在代码中添加次数计数器或限制检查（如 `limit: 1` 或 `usedThisTurn`）。
    - 除非用户明确要求“强制限制次数”，否则默认无限使用。

3.  **极简实现原则 (Minimal Implementation)**:
    - **如果一个技能效果可以通过基本操作（如：手动摸牌、手动弃牌、手动判定）模拟实现，则不需编写复杂的代码逻辑。**
    - 仅实现那些**无法**通过基本操作完成的特殊机制（如：改变卡牌属性、复杂的卡牌转移、特殊标记）。
    - 优先依赖玩家之间的口头沟通和手动操作（面杀模拟器精神）。

## 通用开发原则

1.  **前后端一致性**：
    任何改动都需要考虑是否能真正反映到后端的运行时变量，是否能真正体现到前端中。

2.  **独立实现与非破坏性修改**：
    任何修改都不要影响现有逻辑，不要删除任何现有的代码。任何武将的技能都重新独立实现，不要复用任何模块。

3.  **严禁修改变量名**：
    永远不要修改现有的变量名，这种操作非常危险。

4.  **严禁删除现有逻辑**：
    永远不要删除现有逻辑、现有的选牌框、现有的变量，否则一定会引起大问题！！！！

5.  **增量开发原则**：
    目前测试所有逻辑都恰好正确，后来的任何改动都只能通过增量的形式实现，决不允许修改现有逻辑。

6.  **面杀模拟器优先**：
    以“面杀模拟器”为目标，不需要游戏控制流程，依靠玩家间语言沟通确定“响应杀”“判定闪电”“技能弃牌”等机制；只有当玩家无法实现某一动作时，才补充这部分逻辑。

7.  **随机数一致性**：
    在游戏逻辑（`src/Game.js` 或 `src/skills/*.js`）中，**严禁使用 `Math.random()`**。必须使用 `ctx.random`（如 `ctx.random.Number()` 或 `ctx.random.Shuffle()`）来生成随机数。这是为了确保客户端的乐观更新（Optimistic Update）与服务端的最终执行结果一致，防止出现状态闪烁或不一致的问题。

4.  **服务端校验 (Server-Side Validation)**:
    - **严禁**仅依赖前端 UI 过滤。如果技能要求特定的卡牌属性（如“仅限方块牌”、“仅限黑色牌”），**必须**在 `move` 函数中再次检查 `card.suit`、`card.color` 或 `card.type`。
    - 如果校验失败，应拒绝执行并记录错误日志。

5.  **遗留代码迁移 (Legacy Migration)**:
    - 如果修改的功能位于 `src/Game.js` 的旧代码块中，**必须**将其重构并迁移到 `src/skills/` 下的独立文件中。
    - 保持 `src/Game.js` 的整洁，只保留核心框架和通用逻辑。

## UI/交互规范

1.  **选牌框取消功能**：
    任何选牌框都应该有取消按钮。

## 武将技能开发规范

1.  **忽略 `skills_description` 字段**：
    在开发新武将或修改现有武将技能时，**严禁**参考 `configs/generals.json` 中的 `skills_description` 字段。该字段的内容可能不准确或不符合当前需求。

2.  **遵照用户命令**：
    所有技能逻辑的实现必须严格遵照用户的直接命令和描述。如果用户没有提供具体的技能细节，应主动询问，而不是依赖配置文件中的描述。

3.  **技能逻辑存放位置**：
    任何武将的技能逻辑都应该放在 `src/skills` 目录中。

4.  **技能完整性检查清单**：
    实现一个新技能时，必须完成以下所有步骤：
    - [ ] **逻辑实现**：在 `src/skills/` 下创建或更新技能文件。
    - [ ] **引入技能**：在 `src/Game.js` 中 import 该技能模块。
    - [ ] **定义 Move**：在 `src/Game.js` 的 `moves` 对象中定义相应的 move 函数。
    - [ ] **UI 接入**：在 `src/Board.jsx` 的 `onSkillClick` 中处理点击事件。
    - [ ] **UI 显示**：在 `src/Board.jsx` 的 `renderPlayerArea` 中处理技能名称的动态显示（如需要）。
    - [ ] **测试配置**：将武将加入 `src/Game.js` 的 `TESTING_GENERAL_LIST`。

5.  **技能按钮接入**：
    每次武将技能实现后，必须确保在 `src/Board.jsx` 的 `onSkillClick` 和 `renderPlayerArea` 中正确接入了技能按钮，使得点击技能按钮能触发相应的逻辑或显示正确的状态。

6.  **测试列表更新**：
    每次开发新武将时，必须自动将该武将的名字加入 `src/Game.js` 中的 `TESTING_GENERAL_LIST` 常量中，以便于测试。
