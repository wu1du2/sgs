# Startup Instructions

ALWAYS use the following command to start the application:

```bash
npm run build && npm run start
```

This ensures that the latest changes are built and served correctly.

## Development Rules

1. **Skill Implementation**: All general skills MUST be implemented in a separate file named `[general_name].js` inside the `src/skills/` directory.